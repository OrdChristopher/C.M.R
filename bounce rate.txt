improving bounce rate requires;
secondary pageviews concurrent to a first from the same session,
offering a singular session over a longer time span, and multiple pages,
will create configurable improvements to bounce rate from traffic exchange.